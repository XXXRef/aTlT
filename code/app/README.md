# ATlT — React Frontend

This is a React (JavaScript) port of the original ATlT frontend.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Run dev server

```bash
npm run start
```

Build:

```bash
npm run build
```

Notes:
- All static assets used from the old app have been copied into `public/assets`.
- This project uses Vite and React (JSX).
